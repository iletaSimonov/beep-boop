function beepBoop(number) {
  let result = [];
  for (let i = 0; i <= number; i++) {
    if (i.toString().includes("30"){
      result.push("Wont you be my neighbor?");
    } else if (i.toString().includes("2")) {
      result.push("Boop!");
    })
    
    result.push(i);
  }
  return result;

  
}